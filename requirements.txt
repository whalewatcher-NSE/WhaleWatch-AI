# WhaleWatch requirements.txt
# pyarrow>=18.0.0 is REQUIRED for Python 3.13 (Streamlit Cloud default)
# pyarrow 17 and below crash with "pkg_resources not found" on Python 3.13

nselib
pandas>=2.1.4
numpy>=1.26.4
pyarrow>=18.0.0
requests>=2.31.0
streamlit>=1.32.0
python-telegram-bot>=20.7
